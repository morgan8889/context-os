# Spec Compliance Review: 9118f7107e3e (Phase 2 Foundational)
**Verdict**: PASS WITH NOTES
**Reviewer**: code-reviewer agent
**Date**: 2026-05-19

## Summary

All 17 tasks in Phase 2 (T009–T025) are substantively implemented. The design system tokens,
shared primitives, API client, Zustand store, type definitions, transforms, and app shell are
all present and match the spec. One item requires attention: FilterBar is missing the
mobile-landscape icon-only collapse behavior. The `graphology-layout-dagre` absence is correct
— that package does not exist on npm (removed in fixup ea934ca701bb); dagre layout will use
`@dagrejs/dagre` directly. The unit test scan gap in `src/design-system/` is a note only.

## Findings

### Important

**FilterBar missing mobile-landscape collapse (T015)**

T015 requires Framer Motion collapse to icon button at mobile-landscape breakpoint.
`web/src/design-system/primitives/FilterBar.tsx` has no responsive breakpoint logic — it
renders the full chip row at all viewports. The FR-033 visual regression tests include
mobile-landscape (1024×768) fixtures; the FilterBar at that width may overflow.

Fix at T070 (responsive layout pass): Add a `useMediaQuery` breakpoint check that collapses
the FilterBar to an icon button at ≤430px portrait or landscape.

### Notes (non-blocking)

**T025 test scope gap — does not scan `src/design-system/`**

`web/tests/unit/design-system.test.ts` scans only `src/views/**/*.tsx` for hardcoded color
patterns. No view files exist yet, so no violations will be caught today. Additionally, the
regex patterns block `#hex`/`rgb()`/`hsl()` but not `oklch()` literals, which is the actual
pattern used in primitives. Extend scan at T073 to cover `src/design-system/`.

**T011 — no `tailwind.config.ts` file (correct for Tailwind 4)**

T011 says to configure Tailwind in `web/tailwind.config.ts`. No such file exists. Tailwind
CSS 4 with `@tailwindcss/vite` does not use a config file — configuration lives in the `@theme`
block inside `globals.css`. The CLAUDE.md Phase 3 context confirms this approach. Not an issue.

**T018 — Clerk token injection uses `setTokenProvider` pattern (correct)**

T018 says the interceptor should call `useAuth().getToken()`. The implementation uses a
module-level `setTokenProvider` function wired via `ClerkTokenWirer`. React hooks cannot be
called outside components — this is the correct architectural approach. Functionally identical.

## Task Checklist

| Task | Status | Notes |
|------|--------|-------|
| T009 tokens.css | PASS | All tokens present; all values use oklch() |
| T010 npm dependencies | PASS | graphology-layout-dagre correctly absent (non-existent npm package) |
| T011 Tailwind 4 config | PASS | @theme in globals.css is correct Tailwind 4 approach |
| T012 shadcn/ui components | PASS | Button, Badge, Tooltip, Popover all present |
| T013 StateCTA | PASS | Single data-cta="primary", Framer Motion entrance |
| T014 OverlayPanel | PASS | x:320→0 slide-in, 150ms everyday easing, Escape handler |
| T015 FilterBar | PASS WITH NOTE | Missing mobile-landscape collapse (defer to T070) |
| T016 NodeTooltip | PASS | Radix Tooltip, 500ms delay, 300px max-width, pointer-events none |
| T017 TypeScript types | PASS | DecisionViewState includes 'activating' (QC-008 fix) |
| T018 API client | PASS | setTokenProvider pattern correct |
| T019 QueryKeys + QueryClient | PASS | All 5 key factories; staleTime 60s, retry 2 |
| T020 Zustand store | PASS | All slices; immer; all actions present |
| T021 Transforms | PASS | All 6 functions; bottleneck >500ms; snake_case→camelCase |
| T022 viewState polling | PASS | refetchInterval stops at false when all activated |
| T023 main.tsx | PASS | ClerkProvider→QueryClientProvider→RouterProvider; ClerkTokenWirer |
| T024 router.tsx | PASS | createBrowserRouter; all 4 routes lazy; all protected |
| T025 unit tests | PASS WITH NOTE | Scan gap — extend to src/design-system/ at T073 |
