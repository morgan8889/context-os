# Spec Review — 1727ed0284e6

**Commit**: feat(phase6): seed endpoints, real AGE counts, /onboarding route
**Branch**: 4-closed-beta-readiness
**Spec**: No phase-6 spec exists in specs/. Reviewing against stated commit intent.

---

## Spec Coverage Assessment

No formal spec exists for this commit. The commit message self-documents the scope:
seed endpoints, real AGE counts, /onboarding route. Review is against stated intent
and architectural constraints from CLAUDE.md.

| # | Criterion (from commit message) | Status | Test? | Evidence |
|---|----------------------------------|--------|-------|----------|
| 1 | GET /nodes wired to real AGE via get_nodes_for_tenant | YES | NO | graph.py:355–406 |
| 2 | GET /edges wired to real AGE via run_cypher | YES | NO | graph.py:408–477 |
| 3 | POST /nodes/seed accepts and upserts demo nodes | YES | NO | graph.py:479–527 |
| 4 | POST /edges/seed accepts and upserts demo edges | YES | NO | graph.py:529–572 |
| 5 | views.py initiative_count replaced with real AGE count | YES | NO | views.py:13–28, 77–80 |
| 6 | galaxy_state derived from initiative_count > 0 | YES | NO | views.py:77–80 |
| 7 | /onboarding route added to router | YES | NO | router.tsx:62–69 |
| 8 | OnboardingView seeds 15 nodes + 17 edges | YES (15+17 in DEMO_*) | NO | OnboardingView.tsx:13–60 |
| 9 | Dead Protected wrapper removed | YES | N/A | router.tsx diff |

## Missing Spec / Test Coverage

- No spec document exists under specs/ for this feature.
- Zero tests for the four new endpoints (seed_nodes, seed_edges, list_nodes, list_edges).
- No test for the _count_age_nodes fallback path.
- Constitution principle V (Evaluation-First) requires eval suites before non-dev
  deployment; no evals are present.

## Architectural Constraint Check

- Tenant scoping: seed endpoints pass tenant_id from TenantContext to upsert_node/
  upsert_edge which call _assert_tenant_id — PASS.
- AGE parameter map usage: upsert_node/upsert_edge use the AGE params map, not
  f-string interpolation of user values — PASS.
- Exception: _count_age_nodes in views.py interpolates node_type into a Cypher f-string
  (line 22); see quality review for details.
