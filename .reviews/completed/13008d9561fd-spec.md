# Spec Review — 13008d9561fd

## Changes Reviewed

### src/context_os/api/admin.py
- Adds `POST /admin/integrations/github/connect` accepting `{token: str}`
- Stores encrypted PAT via `OAuthTokenRepository.upsert()` with `tenant.db_tenant_id`
- Returns `{connected: true}` on success; 500 on storage error

### src/context_os/api/graph.py
- `list_nodes`: removes hardcoded `node_type="Initiative"` filter — now returns all node types
- Adds `POST /graph/signals` creating a manual Signal AGE node with deterministic UUID5
- Optionally creates `SIGNALS_TO` edge when `initiative_id` is provided

### src/context_os/api/ingest.py
- Ingest loop pops `_age_label` from node dict, uses it as the Cypher label arg to `upsert_node`
- `node_type` property in the dict remains lowercase for frontend consumption

### src/context_os/ingestion/github/normalizer.py
- `_age_label` carries PascalCase Cypher label (Initiative/Goal/Artifact/Signal)
- `node_type` property now lowercase: project/goal/artifact/signal
- Aligns with galaxy `nodeReducer` which switches on lowercase node_type values

### web/src/views/onboarding/OnboardingView.tsx
- Two-tab layout: "Connect GitHub" and "Sample data"
- GitHub tab: PAT input → POST /admin/integrations/github/connect → POST /ingest/github → navigate to /galaxy
- Sample data tab: preserves existing DEMO_NODES/DEMO_EDGES seed flow

### web/src/inbox/InboxView.tsx
- `GenerateBriefingButton`: POST /briefing/generate → poll /briefing/status/{runId} every 3s → invalidate inbox
- `LogSignalForm`: collapsible, textarea + signal type select → POST /graph/signals
- Error handling: 400 from briefing API shown as "Connect GitHub first" hint

### web/src/views/topology/TopologyEmpty.tsx
- Merges duplicate `style` JSX attributes (TS17001 fix)

## Spec Compliance
- Satisfies plan: GitHub PAT storage, node_type case fix, manual signals endpoint, OnboardingView two-tab, InboxView generate + log
- Encryption via OAuthTokenRepository (Fernet AES-256) — matches plan constraint
- Tenant scoping preserved on all new AGE nodes (tenant_id property)
- Galaxy now sees all node types (project/goal/artifact/signal) — completes the galaxy→real-nodes path

## Verdict: PASS
