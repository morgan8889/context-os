# Spec Review — 658f0f7 (fix(web): memoize galaxy graph + resolve Sigma WebGL colors)

**Branch**: 3-cognition-surface
**Reviewer**: feature-dev:code-reviewer (agent a4cac428)
**Verdict**: approve-with-nits

Both targeted bugs are fixed correctly and conform to the binding Phase 3 spec
patterns.

## Bug 1 — live-graph useMemo (SC-002 / FR-002) — PASS
- Rules of Hooks: all hooks (`useGraphInteractionStore`, `useMemo` ×2,
  `useInfiniteQuery`, `useQuery` ×2, `useEffect`) are called unconditionally
  before the early benchmark return (`useGalaxyGraph.ts:143`).
- `liveGraph` keyed on `[nodesData, edgesData]` (= `nodesQuery.data` /
  `edgesQuery.data`); TanStack Query keeps `data` referentially stable, so the
  graph rebuilds only on real data changes. This is exactly the stable-reference
  contract `ForceLayout.tsx:37-40` (`useEffect([graph])`) needs to avoid
  restarting ForceAtlas2 every render.

## Bug 2 — Sigma WebGL color resolution (FR-008) — PASS
- No `var()`/`color-mix()` reaches the WebGL renderer: `GalaxyActivating.tsx`
  real nodes use `withAlpha(resolveNodeColor(type), 0.5)`, stubs use
  `withAlpha(getCssVar('--color-placeholder-grey'), 0.25)`, and the stub
  nodeReducer matches. All `--color-node-*` and `--color-placeholder-grey`
  tokens are concrete `oklch(...)` values, so resolution yields concrete colors.
- `nodeType` separation maintained (Sigma `type` left unset → default `circle`).
- Sigma reducer pattern (`setSetting('nodeReducer', fn)`) used correctly; no
  React state drives per-node visuals.
- Transform boundary intact — `colors.ts` is renderer-side; API shapes stay in
  the queryFn layer.

## Nit
- **NIT-S-1 (FR-025, single source of truth):** `GalaxyView.tsx:26-29` still
  defines a local `getCSSVar`, duplicating the new canonical `getCssVar` in
  `colors.ts`. Migrate `GalaxyView` to import from `colors.ts` in a follow-up.
  Non-blocking (behavior identical).
