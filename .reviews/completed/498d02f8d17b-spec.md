# Spec Review — 498d02f8d17b

**Commit**: fix(phase4): admin funnel LEFT JOIN, scope idempotency, IngestService.get_job
**Branch**: 4-closed-beta-readiness
**Verdict**: pass — all three changes are spec-compliant improvements

## Changes Verified

### LEFT JOIN activation_events in admin funnel

FR-015 requires timing segments per activated org. The LEFT JOIN ensures `signup_to_connect_ms` and other timing columns are available in the result set. Non-activated orgs return NULL for all timing columns, which is correct per spec.

### post_scope idempotency

FR-005 requires the scope step to be idempotent on retry. Reusing the existing IngestJob when `session.ingest_job_id is not None` satisfies this. No orphaned jobs created on duplicate submissions.

### IngestService.get_job()

Public accessor delegating to the private `_get_job`. Used by `post_scope` for the idempotent path. No spec implications beyond enabling the above fix.
