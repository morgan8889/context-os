# Spec Review — 66507c8 (fix(web): Sigma render-program crash + Phase 3 visual verification enablement)

**Branch**: 3-cognition-surface
**Reviewer**: feature-dev:code-reviewer (agent a38105ad)
**Verdict**: approve-with-nits

## Core fix — PASS
`web/src/views/galaxy/buildGalaxyGraph.ts` correctly leaves graphology `type`
unset (Sigma falls back to default `circle`/`line` programs) and stores domain
type under `nodeType` (nodes) / `edgeType` (edges), with a dangling-edge guard.
Complete and correct fix for the render-program crash.

## Conventions — PASS
- **Transform boundary**: `buildGalaxyGraph` consumes already-transformed
  `InitiativeNode[]`/`InitiativeEdge[]`; no raw `ApiNode` schema leaks into
  renderer code (CLAUDE.md Phase 3 constraint satisfied).
- **Sigma overlay/reducer pattern**: `GalaxyView.tsx:83` nodeReducer reads
  `data['nodeType']`; `getCSSVar()` resolves CSS custom props to computed values
  before handing to WebGL. Composites without re-layout (FR-005).
- **Focused-node memo** (`GalaxyView.tsx:173`) and **ForceLayout snapshot path**
  (`ForceLayout.tsx:68`) both updated consistently to the new attribute scheme.
- **SigmaContainer height**: `minHeight: 0` on the flex wrapper is the correct
  fix for a flex child that must shrink below content height.

## Task coverage
T028/T031/T032/T033/T034/T035/T039 satisfied. T027 (GalaxyActivating) renders
but with a residual color bug (see nits) — partial.

## Nits / follow-ups
1. **GalaxyActivating WebGL colors** (`GalaxyActivating.tsx:42,55,73`): stores
   `var(--color-...)` / `color-mix(...)` strings as Sigma node colors. Sigma's
   WebGL renderer cannot evaluate CSS — real nodes render black, stubs miss
   placeholder-grey. Sibling of the bug just fixed; should use the `getCSSVar()`
   pattern. Affects FR-008 / US1 activating-state visual.
2. **dev_router production gate** (`main.py:176`, `dev_router.py:5`): mounted
   unconditionally; docstring claims a `DEV_SEED_ENABLED` gate that does not
   exist. Least-privilege gap.
3. **devState param** (`viewState.ts:62-68`): binds `s` but returns hard-coded
   `activated` for galaxy/topology — confusing dead code in a dev-only path.
